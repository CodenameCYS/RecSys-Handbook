# DCNv2

DCNv2 通过交叉网络显式构造有界阶数的特征组合，并可使用 mixture-of-experts 的低秩/全矩阵交叉；与深层网络并联时兼顾显式与隐式交互。

适合字段关系复杂且 DeepFM 基线不足的场景。交叉层数、专家数和低秩维度应在固定特征下消融；它不取代候选感知兴趣或多任务建模，且更高参数量需评估线上 P99。

## 常见误解

- **更深的交叉网络必然更好**：交叉深度与参数规模需要受数据量和延迟预算约束。
- **DCNv2 能替代行为兴趣模型**：字段交叉与候选感知历史属于不同问题。