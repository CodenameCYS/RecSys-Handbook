# AutoInt 与 FiBiNET

AutoInt 用多头自注意力选择字段间关系；FiBiNET 以 SENET 重标定字段重要性，再做双线性特征交互。两者适合字段语义复杂、人工交叉覆盖不足的场景。

注意力权重不是因果解释。应在相同 embedding 宽度、字段集合和损失下与 DeepFM/DCNv2 比较，并监控字段缺失、batch 推理开销与长尾字段稳定性。

## 常见误解

- **字段注意力权重就是业务因果贡献**：注意力表达模型内部的表示选择，不等同于因果归因。
- **自动交叉可以忽略字段治理**：字段质量、缺失语义和服务可得性仍是前提。