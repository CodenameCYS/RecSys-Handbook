# 场景自适应

场景自适应可通过场景 embedding、条件归一化、adapter、门控或领域对抗等方式平衡共享与差异。适用于场景间特征分布、候选来源或目标权重显著不同的系统。

选择前先检查按场景训练的基线、样本规模和特征可用性。评测必须按场景、用户交叉场景和新增场景分组；线上回退需要能在适配参数异常时使用共享稳定模型。

## 常见误解

- **场景 embedding 足以解决域差异**：特征、标签、候选来源和目标权重都可能发生变化。
- **全局平均提升代表每个场景受益**：应按场景和跨场景用户分别评测。