# STAR

STAR 为多场景学习共享主网络，并以场景特定参数或星型拓扑保留差异，适用于首页、频道、详情等既共享用户兴趣又分布不同的入口。

先验证场景定义稳定、流量和标签足够，再引入场景参数。要监控小场景被大场景吞没、冷启动、跨场景泄漏与发布版本；场景划分不应替代 L3 的列表规则。

## 常见误解

- **每个入口都应单独训练模型**：共享程度由数据规模、特征重合和目标一致性决定。
- **场景参数可承载页面规则**：列表规则应保持在 L3 的可审计约束中。