# SASRec

SASRec 用单向 self-attention 建模序列的下一步偏好，强调最近行为与长距离依赖。它常用于行为顺序是重要信号、并能获得高质量序列的场景。

训练负例、因果 mask、时间间隔编码和序列截断必须与任务一致。作为 L2 模块时还要将序列表示与用户、物品、上下文、L1 来源特征融合，不能直接把下一物品训练目标当作完整业务效用。

## 常见误解

- **下一物品预测就是完整精排目标**：L2 还需结合候选、上下文和多行为价值。
- **因果 mask 是统一最优选择**：mask 取决于在线可见信息与训练任务定义。