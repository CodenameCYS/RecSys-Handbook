# 模型发布与监控

发布工件包括权重、特征 schema、词表、校准器、效用融合配置、候选接口版本与运行时依赖。流程为离线回放、shadow 比对、小流量灰度、随机实验到全量；每步都应支持分钟级回滚。

监控服务时延/回退、特征缺失/漂移、预测分布/ECE、模型版本占比与业务指标。Shadow 仅验证预测与性能，不替代真实用户实验；业务监控必须关联 L3 后最终曝光和实验桶。

## 常见误解

- **只发布模型权重即可回滚**：词表、schema、校准器和融合参数也属于发布版本。
- **服务稳定就说明效果稳定**：数据漂移、校准偏移和业务护栏需要独立监控。