# 特征服务

在线特征由用户/物品 KV、实时序列、请求上下文和统计特征组成。每次读取必须有 deadline、TTL、缓存、默认值与缺失标记；特征 schema、词表和归一化须与训练版本一致。

服务需监控 RPC P99、缓存命中、缺失率、新鲜度、OOV 和离在线特征哈希差异。超时不可静默返回任意旧值，应显式进入默认值或轻量模型路径。

## 常见误解

- **特征服务只负责读取数值**：它还承担时点、版本、缺失和默认值等语义契约。
- **实时特征一定优于离线特征**：更新时延、稳定性和训练服务一致性决定实际价值。