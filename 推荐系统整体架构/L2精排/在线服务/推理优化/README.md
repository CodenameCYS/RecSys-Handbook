# 推理优化

同一请求候选共享用户和上下文，应批量查询 embedding 并单次前向，而不是逐物品 RPC。容量评测需覆盖候选数、序列长度、字段数、QPS 与 P50/P95/P99。

可使用动态 batching、图编译、量化、算子融合、embedding 缓存、序列截断和蒸馏。每项优化都要在固定特征和候选下复验精度、校准和端到端价值，避免以不可见的效果损失换取吞吐。

## 常见误解

- **量化后只需比较 AUC**：量化还会影响校准、数值稳定性和不同设备上的延迟。
- **单条推理快就代表整页快**：候选批处理、特征读取与尾延迟共同决定请求耗时。