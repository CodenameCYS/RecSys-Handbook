# 级联与降级

L2 可先用轻量模型筛掉部分候选，再对少量候选使用复杂模型；粗层召回率和误杀风险必须以最终目标评测。超时或特征异常时，可回退轻量模型、缓存分或稳定融合分。

降级策略需保留候选语义、模型版本和缺失标记，并演练特征超时、模型加载失败、预测分布突变和 L1 候选不足。安全、合规、频控等硬规则不能因模型降级关闭。

## 常见误解

- **降级只在故障时才需要设计**：容量波动、特征缺失和版本异常都需要可预期的回退。
- **任何排序都能作为降级结果**：降级输出仍需保持基本相关性、资格和接口语义。